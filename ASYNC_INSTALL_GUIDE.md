# 异步依赖安装使用指南

## 功能概述

为了解决 RunPod 上 SSH 连接不稳定导致依赖安装中断的问题，我们添加了后台异步安装功能。

## 核心特性

- ✅ **后台执行**：SSH 断开后，安装进程继续运行
- ✅ **任务跟踪**：随时查看安装进度和状态
- ✅ **结构化日志**：详细的进度信息和统计数据
- ✅ **向后兼容**：不加 `--async` 时行为完全不变

## 使用方法

### 1. 启动异步安装

```bash
python3 volume_cli.py deps install --project speaker-reg --async
```

输出示例：

```
============================================================
🚀 后台任务已启动
============================================================
📋 任务ID: deps_install_20251227_141126_a3f2
📝 日志文件: /runpod-volume/.metadata/tasks/deps_install_20251227_141126_a3f2.log

查看进度:
  python3 volume_cli.py deps status deps_install_20251227_141126_a3f2

实时跟踪日志:
  tail -f /runpod-volume/.metadata/tasks/deps_install_20251227_141126_a3f2.log
```

### 2. 查看任务状态

#### 查看所有任务

```bash
python3 volume_cli.py deps status
```

输出示例：

```
============================================================
📋 任务列表
============================================================

🔄 deps_install_20251227_141126_a3f2
   命令: deps install --project speaker-reg --async
   开始: 2025-12-27T14:11:26
   状态: running

✅ deps_install_20251227_120530_b7d9
   命令: deps install --project speaker-reg --async
   开始: 2025-12-27T12:05:30
   状态: completed

💡 查看详情:
   python3 volume_cli.py deps status <task_id>
```

#### 查看特定任务详情

```bash
python3 volume_cli.py deps status deps_install_20251227_141126_a3f2
```

输出示例：

```
============================================================
📋 任务: deps_install_20251227_141126_a3f2
============================================================
📦 命令: deps install --project speaker-reg --async
⏱️  开始时间: 2025-12-27T14:11:26
🔄 状态: 🔄 运行中

📊 进度:
  当前组: standard (2/3)
  总进度: 66% ████████████░░░░░░

📈 统计:
  ✅ 成功: 2 组
  ❌ 失败: 0 组
  🔄 重试: 0 次

💡 实时日志:
  tail -f /runpod-volume/.metadata/tasks/deps_install_20251227_141126_a3f2.log
```

### 3. 实时跟踪日志

```bash
tail -f /runpod-volume/.metadata/tasks/deps_install_20251227_141126_a3f2.log
```

日志包含：

- 安装进度（当前组/总组数）
- 每个组的安装时间
- 成功/失败状态
- 详细的 pip 输出

## 日志格式

日志中包含结构化标记，便于解析：

```
[PROGRESS] group=pytorch current=1 total=3
[SUCCESS] group=pytorch time=120s packages=2
[FAILED] group=standard exitcode=1
```

## 任务状态

- **running** 🔄：正在运行
- **completed** ✅：已完成
- **failed** ❌：失败
- **unknown** ❓：未知（进程异常退出）

## 目录结构

任务相关文件存储在：

```
<VOLUME>/.metadata/tasks/
├── deps_install_20251227_141126_a3f2.json    # 任务元数据
├── deps_install_20251227_141126_a3f2.log     # 任务日志
└── ...
```

## 常见场景

### 场景 1：SSH 断开后继续安装

```bash
# 1. 启动异步安装
python3 volume_cli.py deps install --project speaker-reg --async

# 2. 记下任务ID（例如：deps_install_20251227_141126_a3f2）

# 3. SSH 断开后重新连接

# 4. 查看任务状态
python3 volume_cli.py deps status deps_install_20251227_141126_a3f2
```

### 场景 2：同时安装多个项目

```bash
# 启动多个异步任务
python3 volume_cli.py deps install --project project-a --async
python3 volume_cli.py deps install --project project-b --async

# 查看所有任务
python3 volume_cli.py deps status
```

### 场景 3：调试安装问题

```bash
# 1. 启动异步安装
python3 volume_cli.py deps install --project speaker-reg --async

# 2. 实时查看日志
tail -f /runpod-volume/.metadata/tasks/deps_install_20251227_141126_a3f2.log

# 3. 如果失败，日志文件会保留，可以查看详细错误
```

## 注意事项

1. **任务 ID 唯一性**：每次运行都会生成新的任务 ID
2. **日志文件保留**：任务完成后日志文件不会自动删除，需要手动清理
3. **进程管理**：后台进程使用 `start_new_session=True`，与当前会话脱离
4. **同步模式**：不加 `--async` 参数时，行为与之前完全一致

## 与同步模式对比

| 特性     | 同步模式      | 异步模式          |
| -------- | ------------- | ----------------- |
| SSH 断开 | ❌ 安装中断   | ✅ 继续运行       |
| 实时输出 | ✅ 直接显示   | ⚠️ 需要 tail -f   |
| 进度查询 | ❌ 不支持     | ✅ 随时查询       |
| 任务管理 | ❌ 不支持     | ✅ 支持           |
| 使用场景 | 本地/稳定连接 | RunPod/不稳定连接 |

## 故障排查

### 任务状态显示 "unknown"

可能原因：

- 进程异常退出
- 系统资源不足
- 权限问题

解决方法：

```bash
# 查看日志文件最后几行
tail -n 50 /runpod-volume/.metadata/tasks/<task_id>.log
```

### 找不到任务

可能原因：

- 任务 ID 输入错误
- 任务元数据文件被删除

解决方法：

```bash
# 列出所有任务
python3 volume_cli.py deps status

# 检查任务目录
ls -la /runpod-volume/.metadata/tasks/
```

## 清理任务文件

任务文件不会自动清理，可以手动删除：

```bash
# 删除特定任务
rm /runpod-volume/.metadata/tasks/deps_install_20251227_141126_a3f2.*

# 删除所有已完成的任务（谨慎操作）
cd /runpod-volume/.metadata/tasks/
# 手动检查后删除
```

## 技术实现

- **后台进程**：使用 `subprocess.Popen` + `start_new_session=True`
- **任务管理**：JSON 元数据文件 + 日志文件
- **进度解析**：结构化日志标记（`[PROGRESS]`、`[SUCCESS]`、`[FAILED]`）
- **状态检测**：通过 `os.kill(pid, 0)` 检查进程是否存在
